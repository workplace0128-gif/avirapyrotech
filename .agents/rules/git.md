# Git Repository Rules

- Always push commits and branches to `workplace0128-gif/avirapyrotech` (which is configured as the default `origin` remote).
- The secondary backup repository is `madhankumarbala2k-afk/avirapyrotech` (configured as `backup` remote).
